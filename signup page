import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

class HomePage extends StatelessWidget {
  const HomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Digital Microfluidics Platform'),
        centerTitle: true,
      ),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(
              'Introduction',
              style: GoogleFonts.nunitoSans(
                fontSize: 24,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 10),
            Text(
              'Digital Microfluidics platforms are recent fluid-manipulation technology which is based on the concept of electrowetting. This concept uses induced voltage to decrease the contact angle of used liquids and manipulate small samples of liquid volumes ranging from microliters to nanoliters along individual electrodes. The miniaturized setup of digital microfluidics platforms helps to reduce the volume of samples used while ensuring performances that can compete with standard laboratory equipment. By implementing machine learning algorithms in digital microfluidics platforms, researchers can optimize the movement of droplets and automate the selection of optimal operating parameters, improving the accuracy and precision of analytical assays. With lab-on-a-chip technology and the help of cloud computing services, clinical care provided to children, especially newborns, can be improved by enabling remote monitoring and control of experiments. In addition, artificial intelligence algorithms can be used to analyze and interpret data generated by digital microfluidics platforms, facilitating the identification of patterns and trends that may not be immediately visible to the naked eye.',
              style: GoogleFonts.nunitoSans(fontSize: 16),
            ),
            const SizedBox(height: 20),
            Text(
              'Sign Up',
              style: GoogleFonts.nunitoSans(
                fontSize: 24,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 10),
            TextFormField(
              decoration: InputDecoration(
                labelText: 'Full Name',
                border: OutlineInputBorder(),
              ),
            ),
            const SizedBox(height: 10),
            TextFormField(
              decoration: InputDecoration(
                labelText: 'Email',
                border: OutlineInputBorder(),
              ),
            ),
            const SizedBox(height: 10),
            TextFormField(
              decoration: InputDecoration(
                labelText: 'Password',
                border: OutlineInputBorder(),
              ),
              obscureText: true,
            ),
            const SizedBox(height: 10),
            TextFormField(
              decoration: InputDecoration(
                labelText: 'Confirm Password',
                border: OutlineInputBorder(),
              ),
              obscureText: true,
            ),
            const SizedBox(height: 20),
            ElevatedButton(
              onPressed: () {},
              child: Text('Sign Up'),
            ),
          ],
        ),
      ),
    );
  }
}
